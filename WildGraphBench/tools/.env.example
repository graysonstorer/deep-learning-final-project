# =============================================================================
# WildGraphBench Environment Configuration Template
# =============================================================================
# 
# Instructions:
# 1. Copy this file to `.env` in the same directory
# 2. Fill in your actual API keys and configurations
# 3. Never commit your `.env` file to version control!
#
# Usage:
#   cp .env.example .env
#   # Then edit .env with your actual values
#
# =============================================================================

# -----------------------------------------------------------------------------
# OpenAI API Configuration (or OpenAI-compatible API)
# -----------------------------------------------------------------------------
# Your OpenAI API key (required for LLM-based data processing)
OPENAI_API_KEY=your-openai-api-key-here

# API base URL (default: OpenAI's official endpoint)
# Change this if using Azure OpenAI, local LLM servers, or other compatible APIs
OPENAI_BASE_URL=https://api.openai.com/v1/

# Model name for data extraction and QA generation
OPENAI_MODEL=gpt-4o

# -----------------------------------------------------------------------------
# Evaluation API Configuration
# -----------------------------------------------------------------------------
# Separate API configuration for evaluation (defaults to OPENAI settings if not set)
EVAL_API_KEY=your-eval-api-key-here
EVAL_BASE_URL=https://api.openai.com/v1
EVAL_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# Jina Reader API (for web scraping)
# -----------------------------------------------------------------------------
# Get your API key from: https://jina.ai/reader
JINA_API_KEY=your-jina-api-key-here

# -----------------------------------------------------------------------------
# Spider API (optional, alternative web scraping service)
# -----------------------------------------------------------------------------
# Your custom spider API URL (if using an alternative scraping service)
SPIDER_API_URL=your-spider-api-url-here

# Output directory for spider results (optional)
SPIDER_OUTPUT_DIR=./output

# -----------------------------------------------------------------------------
# Optional: Proxy Configuration
# -----------------------------------------------------------------------------
# HTTP/HTTPS proxy for API requests (uncomment if needed)
# HTTP_PROXY=http://your-proxy:port
# HTTPS_PROXY=http://your-proxy:port
