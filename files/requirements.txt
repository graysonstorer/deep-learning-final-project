# WikiGame Bot — Python Dependencies

# Core ML
sentence-transformers>=2.7.0       # Dense embeddings (all-MiniLM-L6-v2 by default)
gliner>=0.2.0                       # GLiNER2: zero-shot named entity recognition
torch>=2.0.0                        # PyTorch backend for sentence-transformers + GLiNER

# Graph
networkx>=3.2                       # GraphRAG graph store + community detection
python-louvain>=0.16                # Louvain community detection algorithm

# Web / Wikipedia
requests>=2.31.0                    # HTTP requests to Wikipedia API
beautifulsoup4>=4.12.0              # HTML parsing for Wikipedia pages
lxml>=5.0.0                         # Fast HTML parser backend for BS4

# LLM
ollama>=0.1.8                       # Ollama Python client (for local Llama)

# Utilities
numpy>=1.26.0                       # Numerical ops for embeddings
tqdm>=4.66.0                        # Progress bars
colorama>=0.4.6                     # Colored terminal output
